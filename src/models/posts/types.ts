export type TNewPost = {
    owner: string,
    title: string,
    description: string,
    audio: string,
    image: string,
    category: string,
    commentsAllowed: boolean,
    downloadsAllowed: boolean,
}