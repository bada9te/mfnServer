export type TNewMessage = {
    owner: string,
    type: string,
    text?: string,
    image?: string,
    video?: string,
    audio?: string,
    file?: string,
    spotify?: string,
    reply?: string,
    chat?: string,
    toUser?: string,
    sharedItem?: string,
}